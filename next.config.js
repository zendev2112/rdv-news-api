/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  }
}

module.exports = nextConfig